

<table id="employee">
    <thead>
  <tr>
    <th>Full name</th>
    <th>Designation</th>
    <th>Department name</th>
    <th>Email</th>
   <th><i>Options</i></th>
  </tr>
  </thead>
<tbody>
<% @emp_masters.each do |emp_master| %>
  <tr>
    <td><%= link_to(emp_master.First_Name,emp_master) %>
  <%= emp_master.Middle_Name %>
    <%= emp_master.Last_Name %></td>
    <td><%= emp_master.Designation %></td>
    <td><%= emp_master.Department_Name %></td>
    <td><%= emp_master.Email_id %></td>
  <td><i><%= link_to 'Delete', emp_master, confirm: 'Are you sure to delete?', method: :delete %></i></td>
  </tr>
<% end %>
</tbody>
</table>

<br />

<%= link_to 'New Registration', new_emp_master_path %>
